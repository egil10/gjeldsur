# TLS Configuration
# Set to 1 to bypass TLS verification (for corporate proxies)
INSECURE=0

# Custom CA bundle path (optional)
# REQUESTS_CA_BUNDLE=/path/to/ca-bundle.crt

# Development mode - use mock data if APIs fail
DEV_SEED=0
